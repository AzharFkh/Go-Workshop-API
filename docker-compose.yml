services:
  backend:
    build: .
    container_name: golang_backend_bengkel
    restart: always
    ports:
      - "8600:8600"
    # Docker membaca file .env
    env_file:
      - .env
    environment:
      - GIN_MODE=release # release mode 